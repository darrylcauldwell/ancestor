# Backlog

**The single list.** Every delivery unit lives here — `ROADMAP.md` was merged in on
2026-09-18 and retired, because dependency and ordering spanning two files meant a `gate:`
could point across a boundary with nothing to catch a rename (`#CPU2` → `#RC8` was doing
exactly that), and nothing prevented an ID colliding between them.

Three things are **stored once and never duplicated**:

| | stored | derived by |
|---|---|---|
| **status** | nowhere | `git log --oneline --grep='#<ID>' --all` |
| **dependencies** | forward only, as `gate: #ID` on the blocked item | `grep -rn '#<ID>' AncestorApp/` |
| **what to start now** | nowhere | rows with no `gate:` |

So: no status, no dates, no commit refs, and never an "unblocks" note — the reverse direction
is a grep. A row with commits against it is done: verify its acceptance test, then delete it.

IDs are `#<TAG><n>`, unique repo-wide. Bare `#Change<n>` is deprecated — it collides across
specs (33 commits), so its status cannot be derived.

**Ordering is a rule, not a field.** Priority derives from the declared rule ("core research
capability before polish", owner 2026-07-10), then whether an item is gated, then section
order. No stored priority number, and no sprints — a solo trunk-based developer gets no
coordination benefit from a cadence boundary, and it would be one more thing to keep true.

An item needs an acceptance test once it is ungated. Gated items may carry an outcome only;
clearing the `gate:` is now the whole of "graduation" — no file move, no ID change.

## TT — test target under Swift language mode 6

All four targets now build at language mode 6 with `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`,
defined once at project level. The test target was previously at mode 5 (Xcode template
residue), so its concurrency was never checked. These are the surviving findings.

| ID | Outcome | Acceptance test |
|---|---|---|
| `#TT1` | The `URLProtocol` stub in `Ancestor Research Tests/FamilySearchClientTests.swift` stops fighting actor isolation on its overrides (`canInit(with:)`, `canonicalRequest(for:)`, `startLoading()`, `stopLoading()`, `init(request:cachedResponse:client:)` — lines 276, 314, 315, 317, 333) · **kind:** defect | `xcodebuild build-for-testing -scheme "Ancestor Research Tests"` reports no `different actor isolation from nonisolated overridden declaration` errors |
| `#TT2` | Call-counting test doubles are safe under real concurrency: `FamilySearchClientTests` `record` (lines 71, 97, 122) and `FagBridgeCapTests` `fetchCount`/`fetchedIDs` (lines 183, 184). Decide per double — `actor` with awaited reads, or a mutex — because `RecordSource`/`HTTPClient` are now correctly `nonisolated` and the pipeline calls them from concurrent `async let` children · **kind:** defect | No `cannot be called from outside of the actor` / `can not be mutated from a nonisolated context` errors, and the count assertions still hold |
| `#TT3` | The test build reaches every file. `ProseCorpusAdderTests`, `PublishEngineE2ETests`, `QueryCacheTests` and `ScoreReplayDriftTests` never compiled in the 2026-09-18 runs (build aborted earlier), and still hold Sendable-closure findings plus `conformance of 'Row' to 'Sendable' is unavailable` — GRDB `Row` crossing a concurrency domain · **kind:** defect | `build-for-testing` succeeds, then a full `xcodebuild test` run is green apart from the known `BackupServiceTests` / `MultiWindowAppStateTests` flakes |

| `#TT4` | `Ancestor Research Tests/PublishEngineE2ETests.swift` type-checks in reasonable time. It currently pins one `swift-frontend` at 100% for 7+ minutes as the sole `-primary-file`, which taxes every test build. Earlier runs hid it: a `conformance of 'Row' to 'Sendable'` error at line 163 aborted type-checking before the slow region was reached, so fixing that error exposed rather than caused it (the same is true of my struct-vs-tuple rewrite — it did not clear the stall). The file is only 285 lines, and nothing in it looks obviously pathological by eye, so **measure rather than guess**: rebuild with `OTHER_SWIFT_FLAGS="-Xfrontend -warn-long-function-bodies=5000 -Xfrontend -warn-long-expression-type-checking=5000"`, which names the offending function and expression with source locations. SDK 27 regressed type-check performance on deeply-branching content (Apple's `content-builder.md`), so the trigger may be the toolchain rather than the code · **kind:** defect | A clean `build-for-testing` compiles this file in seconds, not minutes, and the fix is recorded so nobody reintroduces the shape |

## GL — Liquid Glass

macOS 27 adds no new glass API (verified against the macOS 27.0 SDK: `Glass`, `glassEffect`,
`GlassEffectContainer`, `ConcentricRectangle` are all still macOS 26.0). The improvement is
system-side rendering, so the work is using the existing API properly. Measured 2026-09-18:
49 hand-rolled glass card rects across 25 files, 36 of them the `.padding(N)` +
`.glassEffect(.regular, in: .rect(cornerRadius: N))` idiom, 5 radii against 5 paddings with
no rule, and no shared card component.

| ID | Outcome | Acceptance test |
|---|---|---|
| `#GL1` | A `GlassCard` component plus shape/spacing tokens (mirroring the `AppTypography` no-inline-literals rule), with the 36 idiom sites migrated to it · **kind:** hygiene | No bare `.glassEffect(.regular, in: .rect(...))` outside the component; the radius/padding set is reduced to a documented few and named in the tokens file |
| `#GL2` | Glass grouped into `GlassEffectContainer` at the seam rather than per site, including the open question of one container above the page-level `LazyVStack` (shares one backdrop pass across every card; risks breaking laziness from above, which is the documented cause of the Triage scroll hang) · **kind:** hygiene | `sample` during a Triage scroll on a large cluster shows no more `AttributeGraph`/`QuartzCore` heat than today's build, and visibly fewer backdrop passes |
| `#GL3` | Nested glass corners derive from their container via `.rect(corners: .concentric)` / `ConcentricRectangle` instead of hardcoded radii · **kind:** hygiene | A capsule inside a card visually shares the card's corner geometry at both card sizes |
| `#GL4` | The HR4 severity ladder carries its band in the material — `.glassEffect(.regular.tint(.red/.orange/.blue), ...)` — instead of colouring text on neutral glass · **kind:** capability | Red/amber/blue bands are distinguishable at a glance in both light and dark appearance |
| `#GL5` | Settle whether macOS 27 fixed the Liquid Glass hit-testing bug where a `.glassEffect` capsule in a Button label ate the click, and delete the dedicated-chevron workaround in `ClusterReviewView.recordRow` if so · **kind:** verification | A single Button wrapping a whole record row registers the click on macOS 27; the workaround comment and extra chevron Button are removed |

## M27 — macOS 27 API adoption

Deployment target stays `26.2`, so **adoption** rows are gated behind `if #available(macOS 27, *)`.

`#M27-5`–`#M27-7` are different in kind: they are *changed semantics of existing API*, which
apply at compile time against the SDK 27 toolchain **regardless of deployment target**, and
cannot be gated away. Apple's own authoritative guidance for these ships with Xcode — read the
matching `references/*.md` before touching one:
`~/Library/Developer/Xcode/CodingAssistant/ExportedPlugins/<xcode build>/claude/skills/swiftui-whats-new-27/`.

| ID | Outcome | Acceptance test |
|---|---|---|
| `#M27-1` | Swipe actions on Workbench Attention rows for accept / discard / refuse without opening the row. SDK 27 allows these in **any scrollable container** — mark the container `swipeActionsContainer()` and keep `swipeActions(edge:allowsFullSwipe:)` per row — so the existing `ScrollView`/`LazyVStack` needs no rewrite into a `List`. See `references/swipe-actions.md` · **kind:** capability | Swiping an Attention row on macOS 27 performs the action; on macOS 26 the row behaves exactly as today |
| `#M27-2` | A decision, written into `RESEARCH_PIPELINE_SPEC.md`, on whether dragging a person between clusters should exist at all given "when in doubt, split" — and if yes, `reorderContainer(for:)` / `reorderDestination(for:in:)` implementing it · **kind:** capability | The spec records the ruling with its reasoning; any implementation cannot merge two clusters without the existing confirmation path |
| `#M27-4` | `#Preview(arguments:)` parameterised previews across the 106 view files, starting with the states that are painful to reach by hand · **kind:** hygiene | A reviewer can see empty / populated / conflict states of a view without driving the app |

| `#M27-5` | The `@State`-as-macro migration is understood and guarded. `@State` is no longer a property wrapper; views that compiled before can fail with "variable used before being initialized", "invalid redeclaration of synthesized property", or "extraneous argument label". 474 `@State` declarations here. **Apple states the intuitive fix — reordering init assignments — is WRONG and produces incorrect runtime behaviour**; `references/state-macro.md` has the correct one · **kind:** defect | The app and both viewer shells build against SDK 27 with no `@State` diagnostics, and the correct remedy is recorded in `CLAUDE.md` gotchas so nobody applies the wrong one |
| `#M27-6` | The 9 `confirmationDialog` files and 14 `.alert(` sites move to the new `item: Binding<T?>` overloads where they currently drive presentation from a Bool plus an optional. This is the `.sheet(item:)` shape that already fixed the EmptyView-rectangle bug (memory `feedback_sheet_isPresented_race`) · **kind:** hygiene | No `isPresented:` + `if let` pairing remains in an alert or confirmation dialog; each presents from a single optional binding |
| `#M27-7` | The `@ContentBuilder` unification is absorbed. It is source-incompatible for ambiguous `ShapeStyle` overloads in `overlay`/`background` and for type references shadowed by other modules — the same family as the "ShapeStyle ternary fails with mixed types" trap already in memory `feedback_xcode_project_quirks` · **kind:** defect | Build is clean with no `ShapeStyle`/builder ambiguity errors, and any type-check slowdown in a deeply-branching view is identified rather than tolerated |


## DF — dogfood hardening (graduated from ROADMAP 2026-09-18)

Core-correctness fixes from live-tree dogfooding; full repro cases in memory
`project_dogfood_2026_08_06`. "Core research before polish" ranks these ahead of Stage 2.

| ID | Outcome | Acceptance test |
|---|---|---|
| `#DF1` | The second half of the married-surname fix: for a post-marriage-dated census, an unmarried "Dau" of a *same-surname* head is a strong negative — a born-Holmes daughter, not the subject who married into Holmes. Needs household-role analysis, so census-only. The temporal half shipped (`f4c9ab9`) · **kind:** defect | Sarah Gilbert's candidates (all born-WHEELDON girls) score negative rather than clearing; an 1891 "Mary E HOLMES" does not match a subject who became Holmes by her 1915 marriage |
| `#DF2` | Cross-record birthplace-consistency **gate** — applied-fact birthplace vs the candidate/loaded-household target birthplace demotes in ranking/verdicts and gates the census-absorb capsule. Display shipped (`c717fef`); the gate is deliberately still unbuilt because a naive town-compare cries wolf · **kind:** defect | George Ward (@I_1564729976@): applied Ashbourne birth vs the loaded Derby household of James J Ward is suppressed or warned, **and** a legitimate district/town pair like Milford-in-Belper still passes |
| `#DF3` | The geography gate knows registration districts · **kind:** defect | Ellen Brooks' own 1891 Duffield/Milford household row passes instead of soft-failing "unknown district: Duffield" while Derby/Chesterfield/Glossop namesakes pass; Joyce Land's 1945 **Florida** state census is rejected as a childhood census (worst hemisphere breach on record) |
| `#DF4` | The Health Apply-childhood-census path enforces `CensusFamilyLinker`'s guards · **kind:** defect | Joseph W Cauldwell's 1861 census, where he is "Grnson" (3m), does not lift his grandparents as parents; a "Son/Dau" row whose surname ≠ Head's (Joseph CAULDWELL in Joseph REPTON's household) does not propose the Head as biological father |
| `#DF5` | Backing out a namesake census offers cascade-delete of the profiles `addCensusFamily` created for it · **kind:** defect | Undoing the Coleorton/Leics namesake George Brooks household offers to delete the orphaned Ann Brooks island (Ann + Mary/Annie/Edith) rather than leaving a 4-person orphan group |
| `#DF6` | Parent-unlock findings show their evidence at the point of the click — ranked census fields + roster + in-tree badges + the subject's row highlighted, Apply/Dismiss beside it, as the reconciliation panels and absorb capsule already do · **kind:** defect | Re-running the 11-row pre-vet sweep, every trap that made it 0/11 one-click-correct (Grnson grandparents, Ilkeston namesake, Florida 1945, Ireland 1851) is visible without opening the profile or querying MCP |
| `#DF7` | The Add-Relationship "other person" picker disambiguates same-name profiles — rows carry a birth year and top relation · **kind:** defect | The two "Lydia Twyford" and the two "Mary Keyworth" are distinguishable in the dropdown, removing the risk of linking a man to his own daughter |
| `#DF8` | A safe child↔spouse role-change op that preserves the profile and its records, plus the missing parent-younger-than-child audit · **kind:** defect | Herbert Brewell's case is repairable without rename-phantom + delete-real (which destroyed his birth/death/leads research); Mabel b.1897 with son Herbert b.1898 raises a finding |
| `#DF9` | Parent-age-gap severity/midpoint display, so genuinely-approximate imports with wide "abt" windows soften rather than showing red ERROR. The census-derived false positives are already gone (`2657ff4` writes ±1 CAL dates) · **kind:** capability | A wide-"abt" import shows a midpoint/severity view; a real 10-year gap still errors |

## S1 — Stage 1 residual (graduated from ROADMAP 2026-09-18)

| ID | Outcome | Acceptance test |
|---|---|---|
| `#S1-1` | SOURCE_WEIGHTING live verification — the one hard owner-driven app session. Set project Home county, enter Elsie Twyford's known facts, re-run anchored + married-name burial hunt, one anchored run and one Kenneth-class ladder run · **kind:** verification | Dispatch-log query counts compared before/after and recorded in `SOURCE_WEIGHTING_SPEC.md`; staged weighting demonstrably changes dispatch order |
| `#HR5` | Owner walk-through of the recategorised Health tab. HR1–HR4 shipped 2026-08-25 and the severity ladder is documented at `HealthTriage.Key`, but nobody has looked at the result on the live tree. Expect ~176 actionable rows (down from 1,495) led by conflicts then reds, rule chips no longer headed by Completeness and Missing-bio, the ⚡ Quick wins chip present and pinned after All, and Workbench suggestions listing their reasons · **kind:** verification | Health opens on the live tree showing that shape, and any row that contradicts it is captured as its own defect |
| `#SC10` | Live in-app verification of the surface consolidation. All nine SC changes shipped 2026-08-25 (`3356bbb` removed 4,780 lines); the Research tab, Triage tab, Possible People and whole-tree runs are gone and the per-profile card is the review surface, with Workbench Attention as the cross-profile router. Never exercised end-to-end by the owner since · **kind:** verification | A full apply session — find, review, apply, resolve — completes from profile cards and Workbench Attention alone, with no dead entry point and no affordance that the retired surfaces used to provide going missing |
| `#FB1` | One live run confirms the FreeBMD citation backfill's rate-limit behaviour end to end. Changes 1–5 shipped — forward capture, the audit rule, enrich-on-re-research, the throttled whole-tree backfill, and the per-item "Enrich from FreeBMD" Health action. The per-item path exists precisely because the bulk one did 14 of 54 profiles before FreeBMD 429-throttled and the breaker walked to a 900s trip: re-locating a record we already hold is ~1 request per record against ~4 per profile for discovery. That arithmetic has never been observed under a real run · **kind:** verification | A backfill pass over the flagged records completes within one daily FreeBMD window without tripping the circuit breaker, and the request count matches roughly one per record |
| `#PA1` | Attached kin from the non-FreeREG sources reach the tree (parish absorption Change E). Changes A–D absorb the FreeREG typed payload — both parties' parents, witnesses, abodes — but three other sources name relatives that are currently dropped: CWGC casualty records carry "Son of…"/"Husband of…" prose, FamilySearch personas carry attached kin as structured relationships, and probate grants name executors by relationship ("widow", "son"). Find a Grave stays link-only by policy, so its linked memorials become leads, never auto-relations · **kind:** capability | Applying a CWGC casualty that names a parent, and a FamilySearch persona with attached kin, each offers the named relative through the same one-click add path parish absorption uses |
| `#PA2` | Two gaps parish absorption knowingly left. (a) A marriage built from a `co_persons`-only record creates the spouse edge DATELESS — the FreeBMD and parish citations document the marriage, but the edge itself carries no date. (b) The other party's parents are not absorbed onto the subject; they belong to the spouse, and can only be placed once that spouse profile exists · **kind:** capability | Applying a co_persons marriage dates the created edge from the record, and the spouse's parents land on the spouse profile once it exists rather than being discarded |
| `#FR1` | The record-review panel renders the typed FreeREG payload instead of raw fields. `ParishRecord.detail` carries witnesses, occupations, abodes, church and register reference, conditions and literacy marks as structured `FreeREGDetail`, but `ClusterReviewView` still shows the flat raw fields — the richest parish evidence the app holds is parsed, persisted, scored against, and then not shown · **kind:** capability | Reviewing a FreeREG marriage shows both parties' fathers, the witnesses and the register reference from the typed detail rather than raw key/value rows |
| `#FR2` | The typed parish detail feeds inference, not just display. A FreeREG marriage names BOTH parties' fathers — two generations from one record — and a burial carries `relative` plus `relationship` (and a second relative after the chimera fix). Today these are parsed and stored but drive no hypotheses. Gated behind `#FR1` only in the sense that the same payload is the input · **kind:** capability | A marriage record naming a groom's father produces a parent hypothesis for the groom, and a burial naming next-of-kin produces a relationship lead |
| `#FR3` | Three FreeREG wire questions settled against a live run. (a) The exact `<option value>`s the cascading Places select emits for `search_query[place_ids][]` — confirm they are numeric place IDs and establish how a parish name resolves to one. (b) Whether omitting `record_type` for "all" behaves identically to sending the template's explicit `""`. (c) Re-run the trial that returned 0: the cause was FreeREG computing for up to its documented 100s against a 20s transport default, fixed by `FreeREGSource.searchTimeout = 110`, but the re-run that would close it has not happened · **kind:** verification | One live FreeREG trial returns results, and each of the three questions has a recorded answer |
| `#LOC2` | The location picker filters by era (location Part II Slice D). Each `PlaceAuthority` entry carries validity windows — "Wensley & Snitterton" is Bakewell from 1839 and Matlock to 1838 — and the picker's wiring for them exists, but with no populated windows it filters nothing, so a user picking a place for an 1841 event is offered districts that did not exist then · **kind:** capability | Choosing a place for an event dated 1838 offers Matlock and not Bakewell for Wensley & Snitterton, and an undated event still offers both |
| `#LOC3` | FamilySearch place-authority enrichment (location Stage 4). Load FS place ARKs against the local gazetteer so a coded place carries an external identity rather than only an internal id. Gated on the production-environment flip (`#FS6`) — 53k places were already crawled into `fs-place-ids.json`, which remains uncommitted pending that verify · **kind:** capability | A gazetteer entry resolved from an FS place ARK round-trips to the same entry on a later run, and the crawl output is committed |
| `#SP3` | The subject's consumers read `places`, one at a time (subject-place Slice 3). `ResearchSubject.places: [PlaceRef]` is populated and typed by kind — birth, death, burial, marriage, residence, census — and nothing reads it; every consumer still takes the flattened fields. This is the slice where behaviour first moves, so each consumer moves behind a replay diff proving the accept/reject decisions did not change · **kind:** capability | Each moved consumer produces a byte-identical replay verdict set against the captured baseline, or a deliberate, reviewed difference |
| `#SP4` | The flattened location fields are deleted (subject-place Slice 4). Only once every reader has moved to `places` under `#SP3` — until then the two representations coexist and can disagree · **kind:** hygiene | No consumer references the flattened subject location fields, and the suite is green with them removed |
| `#SP5` | Census and residence counties reach every source — the gap that started the subject-place work (Slice 5). A subject's census and residence places are known but do not propagate to every connector's query axes, so searches stay narrower than the evidence allows. Widening search changes results, so by the spec's own invariant it ships behind the replay diff · **kind:** capability | A subject with a census county distinct from their birth county has that county in the emitted axes of every source that accepts one, with the replay diff reviewed |
| `#LOC4` | An owner pass over the location surfaces, judged on one criterion: does the app distinguish what it KNOWS from what it is GUESSING? Every failure the 2026-08 location work fixed was the app sounding certain when it wasn't. The shapes worth exercising, from the retired UAT script: a place whose name exists in two counties (the Middleton case the work was built for — expect a low score, rivals named, and the wrong district shown struck through); two distinct real places sharing one name, versus one place spelled two ways; deciding and recording a place; the escape hatches when the gazetteer is wrong; the picker; and the honest gaps — a marriage place typed inline on a tree card and a burial Cemetery field both still take plain text with no structured code, and Suspect Location checks formatting only, so `Notaplace Magna` passes it. A fresh script must be derived from the current build: the 2026-08-18 one was retired as stale, pinning per-row expectations from before the Health recategorisation and before the late-August dogfood runs changed the rows it named · **kind:** verification | The pass completes against the current build, each divergence recorded as its own row |
| `#DOC1` | Every orphaned spec in `AncestorApp/` is either retired or listed in `README.md`. Twelve documents sat on disk outside the index. Their remainders have been extracted into rows (#BA1–2, #HR5, #SC10, #FB1, #PA1–2, #FS5–6, #FR1–3, #LOC1–4, #SP3–5); what remains is the owner's retire/keep call on each and the README update that follows. Keep candidate: `FS_WRITE_WIRE_CONTRACTS.md` alone (a verbatim external API capture we cannot re-derive). `UAT_LOCATION_SURFACES.md` is retired as stale, its durable intent carried into `#LOC4` · **kind:** hygiene | `ls AncestorApp/*.md` and the README index name the same set of documents |
| `#S1-3` | §14.B.2–6 MCP auto-approval Phase 2 — the transaction/undo keystone, a cross-package build (app + standalone MCP raw-SQL). Auto-approval stays OFF until the undo lands · **kind:** enabler | An MCP-committed fact can be undone transactionally from the app; `#RC1` (corroboration Change 5) is unblocked by the keystone covering relationship entities |
| `#DC6` | Mid-run adaptive anchoring sees post-exclusivity verdicts. `RecordScorer.applyExclusivity` runs at batch assembly, so during a run the adaptive anchoring step still reads pre-pass `.fact` verdicts and can steer subsequent searches off a namesake that the pass will demote. Deferred deliberately in `DECISION_CORE_PAIR_SPEC` (#DC1–DC5 as-built) because a full mid-run reflow was judged too invasive — the persisted result and everything downstream already see post-pass verdicts, so this affects search steering within a run, not stored data · **kind:** defect | A run whose early batch contains a namesake later demoted by the exclusivity pass does not issue follow-up searches anchored on that namesake; the dispatch log shows anchoring keyed to post-pass verdicts |
| `#CPU3` | Census parent-unlock is live-verified end-to-end on the real tree — an owner-driven app session, never done. Changes 1–3 are built and unit-tested but the whole path has only ever run offline. The worked case is George Keyworth b.1838 Farnsfield (Notts), whose 1851 Halam census the ranker picks over Carrington and Middlesex namesakes · **kind:** verification | On a parentless profile with a rankable childhood census, Health's "Apply childhood census" applies the ranker's winner and "Add census relatives" proposes Head + Wife as parents, which land after review. If George's own 1851 Halam lead has no stored household the run is blocked by `#CPU1` — pick another parentless profile rather than treating that as a failure of this item |
| `#CPU2` | `ChildhoodCensusRanker` ranks village adjacency, not just county match. Today a census in a neighbouring village scores no better than one anywhere else in the county — Halam↔Farnsfield is the worked case from George Keyworth (1838). Explicitly a non-goal for v1 in the retired `CENSUS_PARENT_UNLOCK_SPEC` because no village→district data existed. **gate: `#RC8`** (village-level gazetteer expansion) · **kind:** capability | With village data present, a same-village or adjacent-village childhood census outranks a same-county one at equal age fit; George Keyworth's Halam census still wins, and no out-of-county namesake is promoted |
| `#TNS1` | `MemorialInscriptionRecordSource` is live-verified end to end. Stage 2 shipped source + dispatcher wiring tested against fixtures only; the spec records a real run as owed. Same class as `#CPU1` — see the no-blind-scraper rule: fixtures prove the parser handles the shape you imagined, not the shape the site serves · **kind:** verification | One paced GET against the live volunteer site returns HTML the parser turns into burial records with birth years, and the pacing is polite — ONE call, not a loop |
| `#TNS2` | Adding a templated site is a user action, not a code change: a UI to paste a URL template, choose a parser (`.memorialInscription` / `.prose`), and record the verified terms — plus bundled configs for GENUKI (`genuki.org.uk/big/eng/{chapman}/{parish}`) and county OPC projects. Stage 3 of the retired `TEMPLATED_NARRATIVE_SOURCE_SPEC`; GENUKI is currently tier-classified in `SourceTierRegistry` but has no templated config · **kind:** capability | A new site can be added from the UI and produces records without a code change, and each bundled config carries a `termsSummary` checked against that site's published terms first (`feedback_verify_source_terms_first`) |
| `#CPU1` | Census parent-unlock closes its live tail: when the winning childhood-census lead has no stored household (it fell outside the per-run enrichment cap), applying it lands the birth/citation but surfaces no parents. One gentle on-demand `fetchDetail` at click time would close it. Deferred until live-verified against a real FreeCEN 200, per the no-blind-scraper rule — and FreeCEN is a volunteer source, so cap at one call · **kind:** capability | Clicking "Apply childhood census" on a lead with no stored household fetches the roster once and proposes Head + Wife as parents, verified against a live FreeCEN 200 rather than a fixture |
| `#S1-4a` | FT-19 FreeREG parish place-scoping — live connector work again now FreeREG is restored to free-trio parity · **kind:** capability | FreeREG queries scope to parish where the subject's parish is known, instead of widening silently |

## EH — error handling

| ID | Outcome | Acceptance test |
|---|---|---|
| `#NS1` | The searched-surface readers apply the same dispute check the negative-search cache does. Guard (e) stops a poisoned negative from *suppressing* a query, but `SourcingReportService` and `CampaignReviewService` still narrate those rows as "searched, found nothing" — so the user is told a search was done that rested on a disputed premise. Worked case: William Gladwin's six FreeBMD marriage negatives were all taken in Nottinghamshire because a disputed birthplace picked the county unopposed, while the marriage sat in Chesterfield RD (7b/741) · **kind:** defect | A negative whose region derives from a field under open dispute is not reported as searched ground on either surface; the Gladwin profile shows those six as unsearched rather than clean-empty |
| `#CMT2` | `Profile.primarySource` is deterministic, or its callers stop depending on it. It reads `sources.values.first?.first?.origin` where `sources` is `[ProfileField: [FieldSource]]` — a Dictionary, so `.values.first` returns an arbitrary element and the answer can differ between runs on identical data. `SourceDefaults` uses it so a new relative inherits an existing person's source, meaning the inherited origin is potentially non-reproducible. Found by the comment audit: the doc said "first-listed", describing an order that does not exist · **kind:** defect | Adding a relative of the same person twice on the same data inherits the same source origin both times; either the field order is made explicit or callers take a defined field |
| `#CMT3` | `SiblingInferenceEngine` does not re-propose siblings already in the tree. Its dedup set was keyed `"\(year)|\(child.birthLocation ?? "")"` but every lookup built `"\(recordYear)|"` — a BMD birth-index entry carries no birthplace — so the guard could only ever match children whose `birthLocation` was nil or empty, and any child with a recorded birthplace was re-proposed as their own sibling. Keyed on birth year alone, the only component both sides hold · **kind:** defect | Running sibling inference against a parent whose children all have birthplaces proposes no child already linked to those parents |
| `#CMT4` | The Swift and MCP accept-flows agree on the mixed strong/weak dedup case. `ProposalDedup.decide` prefers exact given-name matches over surname-only ones (`strongMatches.isEmpty ? weakMatches : strongMatches`); `MCPServer.decideDedup` pools both, so one exact match plus one surname-only placeholder in the same year window yields `.matched` in-app and `.multipleMatches` (create-new, i.e. a duplicate) over `promote_lead`. No test covers the mixed case on either side. Recommendation: adopt the Swift rule — a nameless placeholder ghost should not block dedup against an exact named match, and "when in doubt, split" does not apply when there is no doubt · **kind:** defect | A lead with a given name, promoted against candidates comprising one exact-given-name match and one surname-only placeholder with an overlapping year window, links to the exact match rather than inserting a new profile; a test pins the mixed case on both paths |
| `#CMT6` | `UnifiedTasksView` sheds its dead audit-finding branch. `UnifiedTaskAggregator.aggregate` no longer emits `.auditIssue` — audit findings moved to Health and `auditSummary` stays in the signature only for call-site stability — but `UnifiedTask.auditIssue`, its title/icon/tint/profileID arms, the rule-ID sub-filter, the per-rule counts and the `.gap` "Research" button all remain, unreachable. Dead view code that reads as live routing, and it already misled a comment audit into believing Tasks routes findings by category · **kind:** hygiene | `UnifiedTask` has no `auditIssue` case, `aggregate` no longer takes `auditSummary`, and the Tasks view builds and renders identically |
| `#CMT7` | The soft-limit name warning reaches the user, or goes. `AutoSuggestService.nameWarning` returns "Unusually long (N characters). Did you paste extra text?" above `nameSoftWarningLength`, but no view calls it — `AddPersonView` and `ProfileDetailView` compare against `nameHardLimitLength` directly to disable Save. The soft band is therefore unreachable in the app and exercised only by `CleanupPassTests` / `NameLengthWarningTests`, which read as coverage of a live feature · **kind:** defect | Typing a name longer than the soft limit but within the hard limit shows the inline warning and still allows save; or `nameWarning` and its tests are deleted |
| `#CMT8` | Accepting a death-year candidate contradicts its rivals, as birth-year and parent-identity accepts already do. In `CandidateGroupCard.acceptSelected` the `.birthYearCandidate` arm calls `db.contradictRivals(inCandidateGroup:acceptedID:)` and the `.parentIdentityCandidate` arm gets the same through `ConflictResolutionActions.chooseParent`; the `.deathYearCandidate` arm calls `ApplyEngine.applyDeathYearCandidate` alone, so rejected death years keep their graded verdict and can resurface as live candidates · **kind:** defect | Accepting one of several death-year candidates leaves every rival in that `candidateGroupID` marked `.contradicted`, and the group does not re-present them |
| `#CMT9` | `AuditEngine.guidanceMessage(for:)` stops advertising a nil it never returns. Every branch returns a string — the no-birth-year case returns the prompt to add one — but the signature is `String?`, so all four test call sites carry `?? ""` and read as if absence were possible. Deferred at comment-audit time because tightening the return means editing those tests and the test target has not built since the Xcode 27 upgrade (`#TT3`) · **kind:** hygiene | `guidanceMessage(for:)` returns `String`, the `?? ""` coalescing is gone from its tests, and the suite is green |
| `#CMT11` | The comment audit reaches the code it never looked at. The 2026-09-18/19 sweeps covered 300 of 883 Swift files: `AncestorViewerKit` (12 files) and `FieldResearcherMCP/Sources` were never swept at all, and the re-sweep was scoped to stale-or-false claims only, so the first pass's other categories are still outstanding — 80 unopenable-reference, 49 archaeology, 8 restates-code, 3 duplicates-implementation, 2 oversized. Those categories are deliberately left to the opportunistic convention in `~/.claude/CLAUDE.md`; this row exists so "not yet looked at" is not mistaken for "clean" Concretely measured 2026-09-19: **41 spec-document references survive in code across 21 files** (FieldResearcherMCP 9, AncestorViewerKit 4, Ancestor-Viewer-TV 3, AncestorKit 2, Ancestor-Viewer-iOS 2, Ancestor Research 1), and **8 of the 11 documents they cite no longer exist** — AUTO_APPROVAL_VIA_MCP, CAMPAIGN_REVIEW, CONFLICT_LAYER, ENGINE_FOUNDATION, MCP_CONSUMER_SURFACE, PHASE4_VIEWER, PUBLISHER and SWIFT_MCP_EVAL_BACKEND. The 2026-09-18 sweep that rewrote ~480 such references never reached these areas. · **kind:** hygiene | Every Swift file outside `.build` has been through a stale-or-false sweep, and the residual categories are either fixed or consciously accepted with that decision recorded |
| `#BL1` | Derived status distinguishes a row that was FILED by a commit from one that was FINISHED by it. `AncestorApp/README.md` and the `backlog` skill both say a row with commits against it is done, but when a commit files a row and cites the new ID in its subject — as `#CMT2` and `#CMT4` were — the row reads as shipped while the defect is untouched. Fix by convention (a filing commit must cite the work it arose from, never the ID it creates) and/or by teaching the skill to discount a commit whose diff ADDS that row to `BACKLOG.md`  A second failure mode, hit while filing these very rows: derived status is BINARY — zero commits means open, one or more means done — so it cannot express multi-commit work in progress, and any umbrella ID cited across a series of commits reads as finished from the first one. The practical convention that falls out is to tag such a series with a workstream tag that has no row (as `#CMT1` does) and give the row itself a distinct ID. · **kind:** defect | Running the skill over a row filed in the same commit that cites it reports it open, and `#CMT2` / `#CMT4` render as open |
| `#BA1` | Age-implied birth corroboration tolerates honest age slop. `AbsorptionPlan` appends `ApplyEngine.impliedBirthDate` unconditionally, so a death/burial whose age-implied birth is only 1–2 years off a precise birth opens a `birthDate` dispute that is noise — a headstone or registration age drifts that much honestly. Treat within-tolerance as agreement rather than conflict. A refinement of the absorption/conflict layer, not a new audit rule (the detection itself already exists end-to-end) · **kind:** capability | Applying a burial recording "aged 72, d. 1962" to a profile born 1889 adds the corroboration without opening a dispute; a 4-year disagreement still opens one |
| `#BA2` | Rejections judged against a superseded birth anchor get re-opened. When the user materially re-anchors a birth (moves it by more than the tolerance), records rejected under the old anchor were judged on an assumption that no longer holds — Mary Lizzie Ward accumulated ~84 discards against a birth year that was wrong by 3 years. Offer to reset `user_status` `discarded → unreviewed` and clear the `record_rejections` row, scoped to birth-year-dependent types (birth/baptism/christening, age-bearing census and death) so unrelated rejections stay put · **kind:** capability | Re-anchoring a birth by more than the tolerance offers the birth-year-dependent rejections for review and leaves every other rejection untouched |
| `#EH1` | The 22 discarded-result `try?` calls on **database writes** either handle the error or log it, per the standing rule "never silently swallow errors — at minimum, log them". The compiler catches only the non-Void subset (it flagged 2 of 90 `try?` statements on 2026-09-18), so this cannot be delegated to the build. Highest-risk first, because they sit on the Evidence Firewall review surface where a silent failure means the user believes a decision was recorded when it was not: `PendingFactsReviewView.swift` `updatePendingFactStatus` (×2), `addAcceptedFactProvenance`, `saveRejection`; then `AppState` `clearEvidenceApplied`, `updateEvidenceUserStatus`, `replaceAuditFindings`; then `ResearchViewModel.upsertLead`. The 19 `try? FileManager` cleanup calls are out of scope — discarding those is legitimate · **kind:** defect | No `try?` on a `db.`/`currentDatabase?.` write discards its result without a logged or handled failure path; `grep -rnE '^\s*try\? (db\|currentDatabase\??)\.'` over app + AncestorKit returns nothing outside tests |

## MS — measurements owed

| ID | Outcome | Acceptance test |
|---|---|---|
| `#MS1` | Settle whether app work runs on the main thread during a research run. Before 2026-09-18, `RecordSource.search` and `HTTPClient.get` were inferred `@MainActor` (the properties were all explicitly `nonisolated`, the async methods were not), so calls may have hopped to main. Also covers the open `runrequestwatcher` SQLite-writes-on-main item · **kind:** verification | `sample <pid>` during a run that triggers the marriage fan-out (`async let groomSide`/`brideSide`) shows the main thread idle or in AttributeGraph, not in URLSession completion, HTML parsing or GRDB frames |
| `#MS2` | The glass changes are verified in the running app, not just compiled · **kind:** verification | A Triage screen showing a cluster with a "Possible duplicate" or "Conflicts with tree" badge renders the badge group correctly, and the two collapsed bins ("Scorer rejected", "Discarded") show their count pill distinct from the card behind it |

## WTS — stale workflow worktrees

| ID | Outcome | Acceptance test |
|---|---|---|
| `#WTS1` | The four `.claude/worktrees/wf_53b1eec3-032-*` worktrees (1.4GB) are salvaged or removed, deliberately. All four are 0 commits ahead of main but carry **uncommitted** edits (2/2/6/2 files) — `MCPServer.swift` + a new `GetScoredRecordsTests.swift`, `FamilySearchSource.swift`, `QueryCache.swift`/`SearchDispatcher.swift`, `RecordTypes.swift`. They are based on `3251abc`, so diffing them against today's main measures main's divergence rather than unique work; the real comparison is each worktree's own `git diff` against `3251abc`, then asking whether that change already exists on main · **kind:** hygiene | Every dirty file is confirmed redundant or its change is committed to main, then `git worktree remove` for each and the 1.4GB is gone |

## CFG — build configuration

| ID | Outcome | Acceptance test |
|---|---|---|
| `#CFG1` | A decision on `SWIFT_DEFAULT_ACTOR_ISOLATION`, measured rather than inherited. It came from Xcode's app template, and the service layer overrides it heavily — 212 `nonisolated` occurrences across 228 files in `Services/`, with `ProjectDatabase`, `RecordScorer` and `ProjectStore` all opting straight back out · **kind:** hygiene | Either the setting is kept with the reasoning recorded, or it is removed and the now-redundant annotations are deleted; the app and tests build either way |
| `#CFG3` | Every type that is `@unchecked Sendable` **and** lock-guarded says `nonisolated`. Under MainActor-by-default such a type is silently main-actor isolated while its own declaration claims it is safe from any domain — the contradiction only surfaces when something finally calls it off the main actor. Six known: `FreeBMDQueryShapeTests`, `CWGCQueryShapeTests`, `PerSourceStrictnessTests`, `FreeREGDispatchSelectionTests`, `Ancestor Research/Services/Research/HTTPClient.swift` (`RecordingFormHTTPClient`), `Ancestor Research/Services/Publish/PublishEngine.swift` · **kind:** hygiene | `grep -rl '@unchecked Sendable'` cross-referenced with `NSLock`/`Mutex` returns no type lacking `nonisolated`; app and tests both build |
| `#CFG2` | `SWIFT_UPCOMING_FEATURE_EXISTENTIAL_ANY` enabled, in its own commit · **kind:** hygiene | The project builds with the flag on, with `any` added where the compiler requires it and no behaviour change |

---

# Sequenced and gated

Merged from `ROADMAP.md` on 2026-09-18. These items live behind a gate or a stage ordering; the narrative under each heading is the sequencing logic and is not derivable from any field. An item here needs an acceptance test only once its gate opens — clearing the `gate:` is the whole of "graduation" now, with no file move and no ID change.

# Roadmap — routing document

**Forward-looking only** — shipped work is removed; git history is the archive, and a shipped item whose spec is gone is normal. This file routes what is *still to do*, in what order, and behind which gate. Per-spec change lists stay authoritative on design (see `AncestorApp/README.md` for the document index).

**This file owns sequence and gates. `BACKLOG.md` owns actionable items.** An item lives here while a gate holds it shut; when the gate opens it graduates to `BACKLOG.md` with an acceptance test, keeping its ID. Neither file records status — derive it with `git log --oneline --grep='#<ID>' --all`, or invoke the `backlog` skill. Do not write "shipped" or an "updated" date into this file; both must be maintained by hand and both went stale before (README and this file drifted 6–8 weeks).

Commit refs that appear below mark *prior art* — which half of a partly-done item already landed — for work that predates the ID scheme. New work references its ID instead.

Phases 1–4 (pipeline consolidation, AncestorKit extraction, CloudKit publisher, viewer apps) are shipped. The **release ceremony is deferred until every phase is complete** (user decision 2026-07-10).

**Declared priority (user, 2026-07-10): core research capability before polish.**

Dogfood hardening (`#DF1`–`#DF9`) and the ungated Stage 1 items (`#S1-1`, `#S1-3`, `#S1-4a`, `#S1-6a`) graduated to `BACKLOG.md` on 2026-09-18, keeping their IDs. What remains in Stage 1 is genuinely gated or compound.

## Stage 1 — residual (open)

FT-19 (parish place-scoping) and Location Stage 3 were ungated and are listed above as
`#S1-4a` and — Stage 3 having shipped as `#DC3` — removed entirely. What is left here is
genuinely gated or compound.

| ID | Outcome | Acceptance test |
|---|---|---|
| `#S1-2` | **§7.5 `DeficitQueryResult` 3-state** *(compound — split on pickup: the 9-file refactor is ungated, the T8a pairing is not)* — a careful 9-file contract refactor whose only consumer, T8a, is NEEDS-DARRYL; pair the two. — `RESEARCH_PIPELINE_SPEC.md` · **kind:** capability | — *(write one when its gate opens)* |
| `#S1-4b` | **FT-21 witness-probes** — `CONNECTOR_AUDIT_2026-07.md` · **gate:** a `SourceRecord` record-role model · **kind:** capability | — *(write one when its gate opens)* |
| `#S1-4c` | **T1-C1 dead Cloudflare subsystem** — delete-vs-wire. `CONNECTOR_AUDIT_2026-07.md` · **gate:** owner decision (NEEDS-DARRYL) · **kind:** hygiene | — *(write one when its gate opens)* |
| `#S1-5` | **RESEARCH_PIPELINE Part II tail** *(compound — a bundle of 8+ items with different gates; split into one ID each on pickup. NB T9 also appears as `#RC4`)* — eval-harness Swift/MCP backend (§5.8.8), §5.9 incrementality refactor, §5.10 button collapse, §5.11 hypothesis investigation, T8a/T9/T31/T23, §5.12 five UX passes. — `RESEARCH_PIPELINE_SPEC.md` · **kind:** capability | — *(write one when its gate opens)* |
| `#S1-6b` | **Location Stage 2(b)/4 — village→registration-district `parentID`.** `PlaceResolver` backfills a bare village to its parent parish and registration district. **The gate this row carried — "needs a village→district data source" — was disproven 2026-08-17.** No import is required: the data already ships in `freebmd-districts.json` (Wensley & Snitterton is there under DBY/Bakewell from 1839 and DBY/Matlock to 1838), and four code defects hid it — HTML entities never unescaped on the resolution path, the resolver consulting only `.registrationDistrict` while a census prints the civil parish, compound parishes unreachable by a constituent settlement, and only the first comma segment tried. Those are fixed; the backfill itself was never built · **kind:** capability | A profile whose birth location is a bare village resolves to its parent registration district without the user picking one |

## Stage 2 — sequenced (gate: core declared solid)

Cross-stage rule: DOSSIER (2c) ≥ PROSE_CORPUS Phase B — build the shared `GroundedProseVerifier` once.

## 2a. FamilySearch — Family Tree read/write integration only

**Scope pivot 2026-08-07 (see memory `project_familysearch_beta_program`).** FamilySearch Developer Support confirmed (2026-08-05) that historical-records collection access is **permanently unavailable** to third-party applications through the API — a legal constraint, not a certifiable tier. FamilySearch is therefore a **Family Tree read/write** integration, **not a records source**. The records source + record-hint ingestion were removed from the app on 2026-08-07 (OAuth + tree write leg retained). **All records-enrichment work is struck** (formerly: ARK/persona detail lookup, record-by-ARK, collection metadata caching, collection-level trust tiering, attribution tiering, change-history volatility scoring, negative-search completeness weighting, place-authority enrichment via FS, new records `RecordType` cases, and the FS records/hint follow-ups). Record hinting is likewise dropped — FS surfaces matches only via browser redirect and recommends against building it.

| ID | Outcome | Acceptance test |
|---|---|---|
| `#FS1` | **Certification path** (FamilySearch's stated sequence, in progress): reply confirming our tree read/write direction → FamilySearch sends Beta test instructions → run them (re-verify write **and** read live) → new electronic Solutions Provider Agreement (**read the commercial-use terms against monetisation BEFORE signing**) → production key → Solutions Gallery listing. Gallery listing is the main strategic payoff (targeted discovery channel). · **kind:** capability | — *(write one when its gate opens)* |
| `#FS2` | **Live re-verify the FamilySearch write leg post-decouple** — against a small throwaway test project, never Tree-2. Also exercise the tree READ (person search), which has never had a substantive live test: the 2026-07-30 verification covered the write path only. Rides the Beta test instructions · **kind:** verification | A throwaway project uploads and reads back correctly, and a person search against the uploaded tree returns the expected person |
| `#FS3` | **WF-A change-history sync** — reconcile the local tree against the FamilySearch user tree using FS's Tree Change History feed, with a conflict-resolution UI for divergences. One of the two rows still open on the User-Tree compatibility checklist; schedule once the beta records-index question is settled · **kind:** capability | A person edited on FamilySearch after upload is detected on the next sync and the user is offered the choice of which side wins |
| `#FS4` | **WF-B tree import** — read a FamilySearch user tree into the local database, the reverse of the shipped write leg. Three research prereqs: the wire shape of FS's ternary `ChildAndParentsRelationship`, a normalised `source_descriptions` table, and a gedcomx-date parser port. Mappings follow `GEDCOMX_CONCEPT_MAPPING.md`, which stays live. The second of the two open compatibility-checklist rows · **kind:** capability | An FS user tree imports into a local project with persons, both relationship shapes and source descriptions intact |
| `#FS5` | **WF-C hint source attach-back** — when a FamilySearch hint is accepted locally, attach the corresponding source back to the FS person through the Source Linker flow, so the contribution is visible on their side rather than only ours. The write surface it needs already exists: `createSourceDescription` plus the three attach endpoints shipped in WL1 · **kind:** capability | Accepting an FS hint on a profile that is linked to an uploaded FS person leaves a source attached to that person on FamilySearch |
| `#FS6` | **WF-D production environment flip** — move off `apibeta.familysearch.org` to `api.familysearch.org` with an embedded production key. Gated on certification (`#FS1`); until then everything runs against beta, and the uploaded trees (group `9M9J-9QZ`, tree `9NMM-98CV`) live there · **kind:** enabler | A tree uploads to the production host with the production key and reads back correctly |

## 2b. Source access compliance (gates FreeCEN scoping + red-source connectors)

| ID | Outcome | Acceptance test |
|---|---|---|
| `#TOS1` | **ADR-008 decision** — owner accepts a compliance posture for the 3 red charity sources + CWGC (gates the rest of 2b + FreeCEN scoping). — `SOURCE_ACCESS_COMPLIANCE_2026-07.md` / `ADR-008` · **kind:** enabler | — *(write one when its gate opens)* |
| `#TOS2` | **Free UK Genealogy permission email** (one email covering FreeBMD/FreeCEN/FreeREG). — owner · **kind:** enabler | — *(write one when its gate opens)* |
| `#TOS3` | **CWGC licensing email** — after the Free UK Genealogy email. — owner · **kind:** enabler | — *(write one when its gate opens)* |
| `#TOS4` | **Manual terms checks** — FindAGrave terms, Probate gov.uk footer, Wirksworth conditions + John Palmer contact. — `SOURCE_ACCESS_COMPLIANCE_2026-07.md` · **kind:** enabler | — *(write one when its gate opens)* |
| `#TOS5` | **Connector gating** — off-by-default for unsanctioned red sources + the ADR-008 outreach/toggle leg. — `ADR-008` · **kind:** capability | — *(write one when its gate opens)* |
| `#TOS6` | **FreeCEN place scoping (Change 6 / FT-13)** — extend `FreeCenParams` with `place_ids[]`, resolve from subject parish/district, honour `.parish`/`.district` natively (stop silent widening to `.county`). — `SOURCE_WEIGHTING_SPEC.md` · gate: ADR-008 + freecen2 published-API docs · **kind:** capability | — *(write one when its gate opens)* |

## 2c. Research capability & narrative

| ID | Outcome | Acceptance test |
|---|---|---|
| `#RC1` | **Cross-profile corroboration — Change 5 (§14 machine-commit carve-out)** — design complete, deferred. — `CROSS_PROFILE_CORROBORATION_SPEC.md` · gate: §14.B undo keystone extended to relationship entities, or corroboration volume that makes the two-click human path a real cost · **kind:** capability | — *(write one when its gate opens)* |
| `#RC2` | **Free-text hunches → targeted probes (§5.15 extension)** — free-text hunch field; local MLX extracts structured directives (death window vs named child's birth, place/occupation discriminators, event kind) into `user_hypothesis_seeds`; deterministic structured-form fallback. Unblocks the parked Harry Marshall acceptance case. — `RESEARCH_PIPELINE_SPEC.md` §5.15 · **kind:** capability | — *(write one when its gate opens)* |
| `#RC3` | **Coal-mining accident databases as a source** (Harry Marshall driver) — terms review (fetch+quote cmhrc.co.uk policy/robots; ask operator if silent) → cheap prose-corpus value probe → structured connector ONLY if yield + terms permit. — gate: terms-review + free-text hunches landing · **kind:** capability | — *(write one when its gate opens)* |
| `#RC4` | **DOSSIER — T9 investigation dossier + bounded adversarial challenge** (#T9-Change1..6: deterministic dossier + surfaces → challenge detector → emission/steering → MLX Pass B adversarial selection → MLX Pass A smoothing + `GroundedProseVerifier` + cache → lifecycle + eval harness). Build BEFORE/ALONGSIDE PROSE_CORPUS Phase B — Change1/5 build the shared verifier Phase B reuses. — `DOSSIER_SPEC.md` · gate: none (pure additive read to start) · **kind:** capability | — *(write one when its gate opens)* |
| `#RC5` | **Bio synthesis — PROSE_CORPUS Phase B** (B-1 decouple bio from field-source pipeline; B-2 base-layer templates reusing `PublishBioBuilder`/`NarrativeAssembler`; B-3 corpus context retrieval; B-4 MLX synthesis; B-5 verification via the shared verifier; B-6 inline-citation rendering; B-7 regeneration + staleness). — `PROSE_CORPUS_SPEC.md` · gate: DOSSIER lands first/alongside · **kind:** capability | — *(write one when its gate opens)* |
| `#RC6` | **PROSE_CORPUS Phase C** — cluster fingerprint engine (place×time×occupation); harness candidate-URL discovery; in-app candidate-URL review surface. — `PROSE_CORPUS_SPEC.md` · gate: after Phase B has enough bios · **kind:** capability | — *(write one when its gate opens)* |
| `#RC7` | **PROSE_CORPUS open-question tuning (§43–47)** — pivot-score weighting, page-split threshold, stop-word list, applicability window, verification depth. — gate: real built corpus data · **kind:** capability | — *(write one when its gate opens)* |
| `#RC8` | **Village-level gazetteer expansion** — broad village coverage, GENUKI's ~12k settlements, widening which place strings resolve at all. Distinct from `#S1-6b`, which resolves villages the catalogue ALREADY holds; this adds ones it does not. The "needs a village→district source" gate previously on this row applied only to the `parentID` half and was disproven 2026-08-17 — see `#S1-6b` · **kind:** enabler | A sample of villages absent from `freebmd-districts.json` resolve to a place-authority entry with a parent district |

## 2d. Kinship (Stage-2 first item by ADR-007, but respec-gated, lowest-urgency)

| ID | Outcome | Acceptance test |
|---|---|---|
| `#KIN1` | **KINSHIP Swift-first respec** — rewrite #Change3–5 as a Swift plan (mandated gate before any build); FIRST — blocks the rest. — `KINSHIP_SPEC.md` · gate: "core declared solid" (ADR-007) · **kind:** enabler | — *(write one when its gate opens)* |
| `#KIN2` | **`find_spouses` primitive (Swift)** — all-spouses w/ per-spouse evidence. — gate: respec · **kind:** capability | — *(write one when its gate opens)* |
| `#KIN3` | **`find_siblings` / `find_children` (Swift)** — gender-asymmetric. — gate: respec · **kind:** capability | — *(write one when its gate opens)* |
| `#KIN4` | **`discover_kin` fan-out walker + `KinshipGraph`** — depth caps, per-edge evidence, `presumed_living`. — gate: find_spouses + find_siblings/find_children · **kind:** capability | — *(write one when its gate opens)* |
| `#KIN5` | **`verify_relationship` + `KinshipVerdict`** — chain-tracing verification. — gate: find_siblings/find_children · **kind:** capability | — *(write one when its gate opens)* |
| `#KIN6` | **Living-person guard extension** — into all primitives; hallucination fixture. — gate: alongside discover_kin · **kind:** capability | — *(write one when its gate opens)* |
| `#KIN7` | **Kinship harness** `--mode discovery\|verification` + recall/precision metrics. — gate: discover_kin + verify_relationship · **kind:** capability | — *(write one when its gate opens)* |

## 2e. Media & publisher

| ID | Outcome | Acceptance test |
|---|---|---|
| `#MED1` | **source_media Option A/B decision** (extend `attachments` vs new firewall-parallel `source_media` table; both lean B). — `SOURCE_MEDIA_SPEC.md` · **kind:** enabler | — *(write one when its gate opens)* |
| `#MED2` | **`source_media` migration + `SourceMediaCandidate` model + `ProjectDatabase+SourceMedia.swift`.** — gate: A/B decision · **kind:** capability | — *(write one when its gate opens)* |
| `#MED3` | **Find a Grave headstone/gallery extractor** (URL-only) + **Inspector UI** (disclosure + per-row on-demand Download); highest-yield first cut. — gate: source_media migration · **kind:** capability | — *(write one when its gate opens)* |
| `#MED4` | **Storage lifecycle** — `fetchStatus` urlOnly→cached + auto-cache heuristics + "cache all" toggle. — gate: source_media migration · **kind:** capability | — *(write one when its gate opens)* |
| `#MED5` | **CWGC image extractor** (headstone photo + certificate PDF URL). — gate: FAG extractor · **kind:** capability | — *(write one when its gate opens)* |
| `#MED6` | **FamilySearch image extractor** (decode `links[]` on `GxSourceDescription`, image-waypoint URL + region). — gate: FAG + CWGC extractors + FS session · **kind:** capability | — *(write one when its gate opens)* |
| `#MED7` | **Publisher `convergenceByProfile` feed** — Sourcing verdicts feed the publisher's per-profile convergence badge. — `SOURCE_WEIGHTING_SPEC.md` · independent · **kind:** capability | — *(write one when its gate opens)* |

## 2f. Assistant surface (MCP consumer)

| ID | Outcome | Acceptance test |
|---|---|---|
| `#ASST1` | **In-app "Connect to Claude" button** — the MCP server + `.mcpb` Desktop Extension artifact are built (`Scripts/build_mcpb.sh` → `dist/AncestorResearch.mcpb`, reader posture baked in). Remaining: live install-verify in Claude Desktop, then embed the server binary in the app bundle (Xcode Copy Files phase, code-sign-on-copy — do with the project open) so the button stages the same zip at runtime. Claude is the assistant tier for now (owner, 2026-07-31). · **kind:** capability | — *(write one when its gate opens)* |
| `#FM1` | **MLX behind `LanguageModelExecutor`** — `LocalInferenceService` conforms to the SDK 27 protocol so MLX drives a real `LanguageModelSession`: `@Generable` guided decoding, tool calling, streaming, transcripts — all on-device. This is the plumbing `#ASST2` would consume. **`PrivateCloudComputeLanguageModel` is forbidden** (outbound calls breach the no-third-party-API invariant). — gate: `#ASST2` intent-plan design, or adopt early if guided decoding replaces hand-parsing sooner · **kind:** enabler | — *(write one when its gate opens)* |
| `#FM2` | **`SystemLanguageModel` fallback tier** — Apple's on-device model so a first run needs no multi-GB MLX download; relevant to App Store first-launch size. — gate: `#FM1` · **kind:** capability | — *(write one when its gate opens)* |
| `#ASST2` | **MLX built-in assistant (bounded)** — the in-app local model drives the SAME reader-posture tool surface in-process: fixed question intents (who-is / evidence-behind / how-related / what's-new / leads-summary) → deterministic tool plans → model narrates returned JSON only (the anti-strategist design). Strategic driver: **iOS — MLX runs on iPhone where desktop MCP clients can't** (iPhone-first market), so this becomes the only assistant tier on mobile; deep investigation stays Claude-over-MCP on Mac. Gate: `.mcpb` packaging + intent-plan design; prereq fix rides along: MLX thinking-off chat-template regression. · **kind:** capability | — *(write one when its gate opens)* |

## Future (no near-term gate / no owning spec yet)

| ID | Outcome | Acceptance test |
|---|---|---|
| `#FUT1` | **WikiTree assisted write-back — BUILT, live-verify BLOCKED on account unblock** (#WT0–#WT4; the sanctioned `Special:MergeEdit` path, where the member commits on WikiTree's own review page — unattended bots are forbidden, so nothing here ever saves automatically). Remaining WT5: Error-2562 unblock (emailed info@wikitree.com) → a small manual edit to prove the unblock → courtesy post in the WikiTree Apps Google Group → first live MergeEdit against Darryl's own or a sandbox profile. Four wire questions can only be settled against a live response: (1) POST encoding — separate form fields with JSON-encoded values (what `WikiTreeMergeEditLauncher` builds) vs one raw JSON body; the Help page shows a single JSON document but a G2G report implies URL params work; (2) whether `person` and the GEDCOM X `data` path may combine in one request; (3) how the review page tolerates uncertain dates like "ABT 1887"; (4) whether an off-apps-server form POST hits third-party-cookie friction — if so, fall back to GET-with-params or host a page in the apps.wikitree.com directory that Apps Project membership grants. · **kind:** verification | A field change contributed from the app appears on the WikiTree review page with its `expected` guard honoured, saves when confirmed, and the next twin sync reflects it |
| `#FUT2` | **Collaborative-tree contribute-then-enrich / `TreeProvider` abstraction** (WikiTree primary, FS secondary) — FUTURE; ADR-006 reversal, gated on a 2nd real tree integration. Both write legs now exist (FS User Trees live-verified; WikiTree MergeEdit built) — the abstraction question becomes real once WikiTree live-verifies. · **kind:** capability | — *(write one when its gate opens)* |
| `#FUT3` | **BYO-API-key frontier-model tier** (Claude/Gemini/OpenAI behind the DOSSIER provider seam) — decision-gated; needs privacy-consent UX + living-people redaction on outbound prompts. · **kind:** capability | — *(write one when its gate opens)* |
| `#FUT4` | **Audit bulk-apply for one-click deterministic fixes** — a "Set/apply all in this category" affordance, gated to deterministic + non-destructive + reversible fixes. Two safe first candidates: **married surname from spouse** (writes only the `married_surname` column; caveat: profiles with a mis-parsed maiden `lastName` still owe a maiden-name follow-up) and **given name contains middle** (clean single-first-name rows only — exclude parentheticals/nicknames → route through `nameJunkResolution`, and compound given names like "Mary Ann"). Keep judgement categories (duplicates, phantom spouse) strictly one-per-row. · **kind:** capability | — *(write one when its gate opens)* |
| `#FUT5` | **Audit: unmatched-location (gazetteer) coverage** — a Health finding for profiles whose birth/death locations have not resolved to a place-authority entry (complements `suspectLocation`, which flags *implausible* places; this flags *unresolved* ones). Gated behind location Stage 3 so it reuses the wired resolver and isn't pure noise. Open sub-questions: low-sev Gap not Issue; whether a one-click "resolve location" fix is offered (needs a disambiguation UI); a Health-screen coverage stat. (MCP `get_profile` doesn't yet expose `birthLocationCode`/`deathLocationCode`.) · **kind:** capability | — *(write one when its gate opens)* |
| `#FUT6` | **Audit "Not a duplicate" per-pair dismissal** — the Duplicate-detection tab offers only Compare→Merge; a confirmed-distinct namesake pair re-fires on every recompute forever (`audit_rule_overrides` scopes no finer than `.profile(id:)`). Sketch: a pair-keyed `rejected_duplicates` table + a suppression check in `DuplicateDetectionRule.evaluate` + a "Not a duplicate" button on the audit row and in Compare. Motivating unclearable cases: George Keyworth ×3 (b.1838/1877/1904), the two Ellen Wards, the two George Wards (b.1851 vs son of William b.1870), Gladys/Glays Cauldwell. · **kind:** defect | — *(write one when its gate opens)* |

## Stage 3 — release (gate: every phase complete — user, 2026-07-10)

First production publish → ASC viewer app records → viewer TestFlight lanes (already written) → family invites → soak items (revocation, unpublish propagation, redaction-as-participant audits).

