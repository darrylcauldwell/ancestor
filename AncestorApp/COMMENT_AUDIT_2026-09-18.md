# Comment audit — comments that contradict their code

63 findings from a 12-agent audit of 881 Swift files (2026-09-18). Category
`stale-or-false` only: a comment that asserts something the code does not do. These are
defects — they mislead a reader into writing a bug — not untidiness.

Each was reported with the code read, but NONE has been independently verified. Confirm
before changing anything; an agent can be wrong about what the code does.

## Source (47)

- `Ancestor Research/Services/Corpus/ProseCorpusCrawler.swift:72` — The comment states the exact inverse of what the code does. `respectRobots` defaults to `true` and `crawl()` does `if configuration.respectRobots { robots = await fetchRobots(...) } else { robots = RobotsTxt(disallow: []) }` — so the default FETCHES AND HONOURS robots.txt, and `false` is what skips it. A reader trusting this doc would believe the shipped default ignores robots.txt on volunteer genealogy servers (a politeness/ToS matter this project cares about) and might 'fix' it by flipping the default. Only the second sentence about tests is accurate, and it accidentally reads as confirming the inversion.
- `Ancestor Research/Services/Corpus/ProseCorpusCrawler.swift:76` — Same inversion as `respectRobots`, on the adjacent property. `useSitemap` defaults to `true` and `crawl()` does `if configuration.useSitemap { let sitemapURLs = await fetchSitemap(...) }` — the default PERFORMS sitemap discovery. The doc claims the default skips it. Reading the two docs together tells a reader the crawler by default neither respects robots nor uses the sitemap, which is the opposite of the shipped behaviour on both counts.
- `Ancestor Research/Services/Corpus/ProseCorpusExtractor.swift:37` — The only stated justification for the 24 KB constant is the context window of DeepSeek-R1 7B, which is not the model this path runs against — `LocalInferenceService` offers Qwen3.5 4B as the reasoning default and DeepSeek-R1 7B is one of several selectable alternatives. The number is therefore sized for a model most runs will not use, and the comment gives a reader no way to tell whether 24 KB is still correct for the model actually loaded.
- `Ancestor Research/Services/Corpus/ProseCorpusSource.swift:430` — The stated condition is arithmetically impossible against the expression one line below: if `surnameHits` is non-zero the score is at least 3, so score can never be zero under the case the comment names as the only way to reach zero. The comment describes a `score == 0` case that cannot exist, so anyone writing a zero-score guard or test from this doc writes dead code.
- `Ancestor Research/Services/GEDCOMDocument.swift:60` — GEDZipWriter IS wired — the next line calls `try? GEDZipWriter.write(...)`. The `try?` now silently swallows a real write failure and ships a .gdz-named file containing plain GEDCOM text; the comment explains that fallback as scaffolding rather than the silent-failure hazard it actually is.
- `Ancestor Research/Services/GEDCOMExporter.swift:28` — The exporter takes a `format: GEDCOMFormat` parameter and branches on `format.version == .v5_5_1` to emit 7.0 as well (lines 70, 99-109, 328) — so the header names only half of what it does. The trailing "See the design" is a dangling reference with the target scrubbed out, pointing nowhere.
- `Ancestor Research/Services/ProjectDatabase+Workbench.swift:4` — Future tense for work already done: ProjectDatabase+Hypotheses.swift, +Focus.swift, +Sessions.swift and +ResearchGoals.swift all exist beside this file. A reader looking for hypothesis persistence is told it does not exist yet.
- `Ancestor Research/Services/ProjectDatabase.swift:52` — The chain is v1…v64: the file registers 64 migrations, the last three being v62/v63/v64, and ProjectStore.swift:35 correctly says "the v1…v64 project migrator". A reader sizing a scratch-DB `upTo:` test against v36 is working from a number the code contradicts.
- `Ancestor Research/Services/Publish/PublishedTree.swift:58` — `bioText` is no longer empty. Sixty lines down, `project(...)` populates it for every `.full` person via `PublishBioBuilder.bio(for:lifeEvents:snapshot:resolved:)`, and `PublishBioBuilder.swift` exists in the same directory as the shipped Change 6 implementation. A reader (or a viewer-side consumer) trusting this field comment would treat published bios as an unimplemented placeholder and skip handling real prose that is in fact being published.
- `Ancestor Research/Services/Research/BiographicalFitEvaluator.swift:17` — `corroboratingMatches` is incremented in three places: rule 2 (line 220), rule 4 census back-calculation (line 283) and rule 5 district affinity (line 306) — the last two documented as corroboration sources elsewhere in the same file. Anyone reasoning about what a count of 5 means will attribute it entirely to death records. "currently" also dates the claim.
- `Ancestor Research/Services/Research/BirthYearConsensusDetector.swift:7` — The routing already exists on this very type: `toPendingFact` (line 60) builds the PendingFact, and ResearchPipeline.swift:536 calls it on every detected consensus. A reader debugging why consensus proposals appear in the review queue is told that path is unbuilt.
- `Ancestor Research/Services/Research/CitationMatcher.swift:14` — A standing maintenance obligation that the project has explicitly retired: the Swift app is the authoritative implementation and is no longer held to parity with the Python agent. The file still exists, so a reader will honour the instruction and constrain a Swift improvement to match reference code that is now background material only.
- `Ancestor Research/Services/Research/ConflictDetector.swift:39` — A shipping-status inventory that the same file falsifies: it now contains `// MARK: - F3` (line 611), `// MARK: - T-D` (line 674) and `// MARK: - F5` (line 714), all implemented. The header reads as an index of what the file does and omits three of its detectors.
- `Ancestor Research/Services/Research/ConvergenceEngine.swift:175` — Twelve lines below, inside the same function, `scoreValueGroups` calls `WitnessIdentity.witnessRepresentatives(of: members)` and scores the representatives — witness counting, already landed. The doc denies the function's central behaviour, and the empty parenthesis "(, stated per spec)" is a stripped reference leaving a broken sentence.
- `Ancestor Research/Services/Research/DisputeResolver.swift:13` — `adjudicate` returns `.rule(id: "R0")` at line 90, `R2a` at 143, `R2b` at 157 and `R2c` at 187 — and the inline comments at lines 72 and 116 say those rungs are "live". The header asserts a zero-write-outcome guarantee the code no longer honours, which is exactly the safety property a reader would trust it for.
- `Ancestor Research/Services/Research/DisputeResolver.swift:30` — Same defect on the type that carries the result: four code paths return a non-nil resolution. The parenthetical tells a caller it may skip handling the non-nil case.
- `Ancestor Research/Services/Research/FreeBMDCitationAudit.swift:115` — FreeBMDSource.swift:911 is percent-decoding of surname/district columns; the id construction the comment points at is now at line 945. A line-number citation into another file goes stale on the next edit and sends the reader to unrelated code — the identifier reference alone would have survived.
- `Ancestor Research/Services/Research/GPSScorer.swift:215` — Two defects in one line: the reference is stripped to a dangling "per;", and the claim is false — the `ConvergenceEngine.scoreValueGroups` this criterion calls already collapses witness families via `WitnessIdentity.witnessRepresentatives`. It tells the reader the score is lineage-counted when it is witness-counted.
- `Ancestor Research/Services/Research/HypothesisEngine.swift:7` — Flatly contradicted by the code beneath it. `generate` dispatches to real implementations for .siblingExists, .parentInferred, .parentMarriage, .subjectSpouseMarriage and .birthYearCandidate (lines 73-85), and `grade` dispatches to eight real graders (lines 105-124), each living in a HypothesisEngine+*.swift extension file with hundreds of lines of logic. A reader is told the entire engine is inert.
- `Ancestor Research/Services/Research/NarrativeAssembler.swift:6` — False. `Ancestor Research/Services/Publish/PublishBioBuilder.swift:71` calls `NarrativeAssembler.templateNarrative(profile:events:)` on the live publish path. The comment tells a reader the type is dead code and safe to delete, which would break publishing. Also short-shelf-life ("currently", a dated sweep).
- `Ancestor Research/Services/Research/RecordScorer.swift:119` — The file is 145 KB and has deliberately diverged (thin-subject verdict cap, #CPC-Change4 cross-profile elevation, registration-district identity resolution, exclusivity ghosts) — none of it in the Python scorer. The repo's own invariant retired Python parity; a reader trusting this would go read agent/scorer.py as the spec for behaviour the Swift no longer shares.
- `Ancestor Research/Services/Research/ResearchPipeline.swift:505` — Contradicted fifteen lines below: the same block, under "Slice B2", calls `writer(consensus.toPendingFact(profileID:))` and increments `state.consensusProposalCount`. Birth-year consensus is not logging-only — it writes a pending fact for review.
- `Ancestor Research/Services/Research/ScoringRules.swift:4` — Same defect as RecordScorer:119, and disproved in this very file: the 0.78 known-spelling-variant rung (added after a 2026-08-23 sweep) and `isSingleIndel` (DS-06) are Swift-only additions to the name-similarity ladder. Line 303's "ported from Python exactly" repeats the claim for the nickname table.
- `Ancestor Research/Services/Sources/FamilySearch/FamilySearchEndpoints.swift:28` — `FamilySearchClient.swift` is a 338-line sibling in the same directory and is what every endpoint here is consumed by. "A later slice" describes a future that arrived; a reader looking for the executor is told it does not exist yet.
- `Ancestor Research/Services/Sources/FamilySearch/FamilySearchEnrichment.swift:17` — The bold label is empty — the words that named the invariant were deleted, leaving a sentence with no subject. Same breakage at `FamilySearchSource.swift:18` ("** pointer-only:**"). The paragraph states a load-bearing rule (FS confidence never sets a trust tier) but no longer says which rule it is.
- `Ancestor Research/Services/Sources/FamilySearch/FamilySearchSource.swift:5` — This 17-line header presents a live records source, but `SourceBootstrap.swift:37-49` records that FS records access is "permanently unavailable to third-party applications", that the type is "deliberately NOT registered", and that it "remain[s] in the tree only as reused internals (GEDCOM X decode); nothing wires them into the pipeline". The only non-test caller is `FamilySearchEnrichment.swift:115` using `parseSearchFeed`. The header carries no hint that its `search` path is dead.
- `Ancestor Research/Services/Sources/FamilySearchAuth/FamilySearchOAuth.swift:36` — "Its" has no antecedent — the environment name this sentence was about was stripped, so the reader cannot tell which of `FamilySearchEnvironment`'s cases is missing or why. Compounded at line 111, where a citation was removed mid-parenthesis leaving the literal text "(, lifetime unverified)".
- `Ancestor Research/Services/Sources/FindAGraveSource.swift:464` — Nothing does this any more. `ensureCloudflareClearance()` (line 474) has zero callers anywhere in the repo, and the same file at lines 103-108 states the opposite architecture: "All FAG fetches go through WKWebView … cookies captured by WKWebView don't carry the TLS profile URLSession then re-presents." A reader debugging a FAG block will chase a cookie path that never executes.
- `Ancestor Research/Services/Sources/FreeREGSource.swift:104` — The code directly below does multi-page fetching. Lines 330-345 read `Self.nextPageURL(html)` and run `while let next = … , pagesFetched < Self.maxPages, records.count < Self.maxResults` to walk the pagination nav, and `maxPages = 10` (line 83) exists solely for that loop. The doc comment tells a reader the connector only ever reads page 1.
- `Ancestor Research/ViewModels/AppState.swift:399` — Two doc comments were merged and the Sourcing paragraph now sits on `placesTabVisible`, which tests birthLocation/deathLocation, not citations. `sourcingTabVisible` twelve lines below (the property the first paragraph actually describes) is left with no comment at all. A reader looking up why the Places tab appears is told about citations.
- `Ancestor Research/ViewModels/AppState.swift:2624` — The body is a one-line forward to `refreshWikiTreeWithDiff()`. There is no direct-refresh path; a caller who picks this function believing it skips the diff gets the diff flow and its pending-diff sheet.
- `Ancestor Research/ViewModels/ResearchViewModel.swift:1113` — `result.proposedRelatives` no longer exists. ResearchState.swift:340 states the field is "now deleted", RunRequestWatcher.swift:351 says "Phase 4: `result.proposedRelatives` was …", and the sibling twin of this comment at line 1337 correctly reads "Phase 4 complete … has been deleted". Only this one still promises a field a reader will hunt for and not find.
- `Ancestor Research/ViewModels/ResearchViewModel.swift:24` — Triage was deleted. The sole consumer of `subjectDisplayName` is Views/Research/ClusterReviewView.swift:337. A maintainer changing this property is told to check a surface that no longer exists and not told about the one that does.
- `Ancestor Research/ViewModels/UserHypothesisViewModel.swift:216` — There is no Triage surface — Views/ has no Triage directory (retired in the 2026-08 surface consolidation); the only consumer of `refutedHunches` is Views/Workbench/UserHunchesView.swift:74. The sentence also ends "per." with its citation stripped, so the justification it points at is unrecoverable.
- `Ancestor Research/Views/Audit/AuditPlaceholderView.swift:4` — The view does NOT group by severity and has no separate open-disputes list. `mergedLadder` (line 382) merges disputes, duplicate clusters, backfill proposals, contradictory facts and findings into ONE flat array sorted by `HealthTriage.Key`, and the view's own comment at line 164 says 'Open field-disputes are no longer a siloed parallel list — they're folded in'. The header describes the pre-#HR4 layout and actively misleads a reader about how rows are ordered.
- `Ancestor Research/Views/ContentView.swift:550` — M14 has shipped in this very file (line 123 'the M14 sensitive-filter toggle, persisted', line 702 'Tiny pre-export sheet (M14)'), yet Cmd+Shift+Z still calls `appState.undoLastTransaction()` identically to Cmd+Z. The comment promises a delivery that its own milestone already passed without, so a reader trusting it will believe redo exists. It is also a bare ticket coordinate (M14) nothing in the repo lets the reader open.
- `Ancestor Research/Views/ManualEntry/ProjectSetupWizardView.swift:11` — Steps 3 and 4 exist in this same file: the private `Step` enum at line 37 declares `case homePerson  // Step 3` and `case sources  // Step 4`, with `// MARK: - Step 3 — Home person` at line 202 and `// MARK: - Step 4 — Sources` at line 228. The header tells a reader two of the four steps are unbuilt.
- `Ancestor Research/Views/Research/PendingFactsReviewView.swift:3` — The view reviews submissions from several producers, and the same file says so: the `AgentFilter` doc at line 17 names prose-extractor and claude-code alongside field-researcher, and the accept path at line 474 exists precisely because in-app producers (`research-run`, `subject-self-narrowing`, `prose-extractor:…`) were being mis-stamped as the field researcher. The one-line header attributes every row to the one producer the file elsewhere warns not to assume.
- `Ancestor Research/Views/Research/ResearchProgressView.swift:17` — `proseBudgetSeconds = 1200` is justified by timings on a model the app no longer defaults to: LocalInferenceService.swift:42 labels `.qwen25_14B` "Previous-generation 14B; superseded by Qwen3.5 4B", and the shipped default is `.qwen35_4B`. The stated basis for the budget no longer matches what users run, so anyone tuning the budget is reasoning from the wrong measurement.
- `Ancestor Research/Views/Research/ReviewWindowRoot.swift:9` — There is no Triage tab: `SidebarTab` (ContentView.swift:682-699) lists tree, tasks, sourcing, places, health, workbench, settings. The load-bearing reason for owning a fresh ResearchViewModel is justified here by a surface that no longer exists, so a future reader cannot check the rationale and may conclude the guard is obsolete.
- `Ancestor Research/Views/Tasks/UnifiedTasksView.swift:5` — The aggregator in the same file no longer produces streams 1 and 2 — `UnifiedTaskAggregator.aggregate` starts at "3. Open questions" and its own comment at line 208 says "Audit issues and completeness gaps now live on the Health tab… `auditSummary` stays in the signature for call-site stability but is no longer consumed here." The file header is directly contradicted twenty lines from the code it describes, and also opens with a dangling "By design" where a spec reference was scrubbed.
- `Ancestor Research/Views/Tasks/UnifiedTasksView.swift:308` — Audit issues and gaps were moved to the Health tab — the same file says so at lines 208 and 429 ("Audit lives on the Health tab now; Tasks is the research worklist") and the filter row at line 472 excludes both categories. The sentence also carries a truncated spec reference ("the design Replaces") that no longer names anything a reader can open.
- `Ancestor Research/Views/Workbench/WorkbenchView.swift:3` — Three claims, all false against the code below it. `SidebarTab` (ContentView.swift:682) has no Audit or Research case. The view's own `Section` enum in the same file already contains attention, focus, hypotheses, hunches, questions, notes, sessions and search — so W3/W4/W5 are shipped, not "future", and it hosts eight sub-views, not two.
- `AncestorKit/Sources/AncestorKit/Profile+PrimarySource.swift:3` — `sources` is `[ProfileField: [FieldSource]]` (Profile.swift:112) and the body is `sources.values.first?.first?.origin`. Dictionary `.values` has no defined order, so there is no "first-listed" field — the returned origin is arbitrary and can differ between runs. The comment promises a determinism the code does not have, and `SourceDefaults` inherits whatever falls out.
- `AncestorKit/Sources/AncestorKit/Research/RecordTypes.swift:760` — The comment states the default is false; four lines below, `public static let countyQueryEnabled = true`. A reader trusting the doc believes FreeBMD is still doing the safe per-district loop when it is in fact emitting county-level queries. The sentence is a leftover from before the probe flipped the gate.
- `AncestorKit/Sources/AncestorKit/Research/RecordTypes.swift:588` — Sources DO read `query.strictness` today — `CWGCSource.swift:210` branches on `query.strictness == .strict` and `FreeCenSource.swift:179` maps it to the `fuzzyFlag` wire value. The comment denies behaviour the code has. It is also a bare change-number roadmap the reader cannot open.
- `AncestorKit/Sources/AncestorKit/Research/RecordTypes.swift:750` — This nine-line paragraph describes an open unknown that the very next line (`PROBED LIVE 2026-07-11`) records as resolved and the gate as flipped on. Two adjacent paragraphs of the same doc comment contradict each other, so the reader cannot tell which describes the shipped code.

## Tests (16)

- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/GeographyGateBMDFallbackTests.swift:6` — RecordScorer.swift:487-507 is now the exclusivity/demotion pass (applied-row discrimination, demote(_:_:)), not the geography-gate BMD fallback the header claims. The line coordinate has drifted and now points a reader at unrelated code. Compounded: 'Mirrors the Python reference' cites an authority CLAUDE.md retired on 2026-07-16 (Swift is authoritative, parity no longer required).
- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/GeographyParishTierTests.swift:10` — That file no longer lives at the implied path (it is AncestorKit/Sources/AncestorKit/PlaceAuthority+Resolution.swift) and the .registrationDistrict predicates are at lines 65-66, 170 and 189 — line 162 is not the cited filter. Both halves of the coordinate are wrong.
- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/GeographyParishTierTests.swift:20` — FreeCenSource.swift:847 is `household: nil  // populated via fetchDetail` inside a record constructor. The actual &amp; unescape is at line 1100. The comment sends the reader to the wrong site for the defect it is explaining.
- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/LadderDedupTests.swift:8` — Wirksworth was retired as a source plugin (CLAUDE.md: 'Wirksworth was retired'; no WirksworthSource exists in the tree) and PerSourceStrictnessTests.swift:233 explicitly records the retirement. The file's own tests cover only probate and findagrave. The header names a third source that cannot be exercised.
- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/MarriedSurnameFloorTests.swift:9` — The only occurrence of marriedSurnameEffectiveFrom in RecordScorer.swift is at line 581; 494-499 is inside the rival-demotion block. The reader following the coordinate lands on code that has nothing to do with DS-18.
- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/ParishAdjacencyTests.swift:38` — RegionConfig.swift's districtParishes map has no "Wirksworth" key — Wirksworth is a parish listed under the Bakewell and High Peak districts. The comment instructs a future maintainer to edit a list that does not exist, so the stated fix cannot be followed.
- `/Users/darrylcauldwell/Development/ancestor/Ancestor Research Tests/PlaceAuthorityRegistryTests.swift:195` — Same drifted coordinate as GeographyParishTierTests:20 — line 847 is the household-field initialiser, not the HTML-entity handling the comment attributes to it. The same wrong number is now duplicated in two test files.
- `Ancestor Research Tests/ConfidenceBadgeViewTests.swift:119` — The legacy `confidenceBadge(_:)` helper no longer exists — ConfidenceDisplaySurfacesTests.swift:13 records its removal, and the very next comment in this same file (line 126) says the `ClusterConfidence` enum was deleted outright. The test body only constructs a `ConfidenceBadgeView`; it verifies no helper at all. It also pins a rotting source coordinate ("line ~875").
- `Ancestor Research Tests/ConfidenceBadgeViewTests.swift:45` — The sentence dangles (the referenced document was edited out), and the claim is false: the test loops over `MatchQuality.allCases` constructing `ConfidenceBadgeView` and contains no `#expect` at all. A reader is told a colour mapping is pinned when nothing is.
- `Ancestor Research Tests/ConfidenceDisplaySurfacesTests.swift:65` — There is no build-time gate. Enforcement is the runtime test below (line 71), which walks `Ancestor Research/Views` resolved against `FileManager.default.currentDirectoryPath` — if that directory is absent under the runner's cwd the enumerator yields nothing and the test passes vacuously. The comment claims a stronger guarantee than the code gives.
- `Ancestor Research Tests/ConfidenceDisplaySurfacesTests.swift:100` — No routing function is invoked. The test computes `cluster.records.contains { $0.verdict == .impossible }` and asserts that boolean — it verifies the fixture, not the routing decision. The parenthetical also preserves what the superseded assertion used to say.
- `Ancestor Research Tests/RecordScorerDateTolerance.swift:5` — The header asserts that ±2 is too loose for birth registrations and that ±1 is correct. `ScoringRules.tolerance(for:)` returns 2 for `.birth` (ScoringRules.swift:129), and this same file's own tests pin that: `toleranceLookupReturnsExpectedTiers` expects `.birth == 2` under a 'Medium tier — ±2' banner (line 87) and `birthRecordTwoYearsOffPasses_atRestoredTolerance` (line 116) asserts the 2-year-off record passes. The header denies the very behaviour the file pins.
- `Ancestor Research Tests/ResearchScopeHierarchyTests.swift:117` — `Profile.birthLocationCode` has shipped and is the FIRST tier of the live chapman-derivation chain (`ResearchSubject.swift:1557`, `chapmanCodeFromLocationCode(profile.birthLocationCode)`), with a whole suite pinning it (`ResearchSubjectHomeChapmanDerivationTests`, 'Tier 1 — birthLocationCode (gazetteer ID)'). A reader is told a shipped field does not exist and that these two tests are temporary scaffolding.
- `Ancestor Research Tests/ResearchScopeHierarchyTests.swift:9` — `ResearchScope` has six cases (parish, district, county, adjacent, national, international — ResearchSubject.swift:83-95); the test beneath is literally named `ac3_1_sixCasesInWideningOrder` and asserts `allCases.count == 6`. The section header states a number the code and the test on the next line both contradict.
- `Ancestor Research Tests/StableRecordIDTests.swift:5` — There is no Wirksworth source: `Ancestor Research/Services/Sources/` contains no WirksworthSource, `SourceBootstrap` registers none, and the project CLAUDE.md records it as retired. The only 'Wirksworth' occurrences in this file are a place name inside FreeREG HTML fixtures (lines 30, 59) — no test exercises a Wirksworth record ID. Half the stated subject of the file does not exist.
- `Ancestor Research Tests/UnifiedTaskAggregatorTests.swift:206` — Both named surfaces are gone. The surface consolidation retired the Triage tab and the Possible People panel — recorded in this same test target by `RetiredSurfaceCopyTests` (which scans view sources for the literal strings 'Triage' and 'Possible People' precisely to keep them from reappearing) and in `ProfileDetailView.swift:81` / `UnifiedTasksView.swift:16`. The comment directs the reader to a destination that no longer exists.
