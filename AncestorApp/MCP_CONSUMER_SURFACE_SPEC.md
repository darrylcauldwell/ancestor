# MCP_CONSUMER_SURFACE_SPEC — read + research-trigger assistant surface

**Status:** Accepted 2026-07-31 (owner: "implement all of these three tiers"). Build MC1–MC6, each one commit, gated on `swift test` (FieldResearcherMCP) and `xcodebuild test` where the app is touched.
**Basis:** coverage audit wf_4be06a27-8c2 (memory `project_mcp_consumer_surface` carries the full ranked findings; file:line evidence there). Direction: the shipped app becomes readable by the user's own LLM over MCP, and the LLM can TRIGGER research — but no tool that writes to the tree ships. Firewall intact; results reviewed in-app.
**Out of scope here (later phase):** .mcpb Desktop Extension packaging + the app's "Connect to Claude" button.

## Slices

- **MC1 — Tier-1 correctness** (MCPServer.swift): (a) `search_profiles` tokenised AND-matching against the concatenated name (+ `married_surname`) — fixes multi-token queries; (b) `is_deleted = 0` on `allProfiles` + `treeGaps`; (c) implement the advertised-but-empty `find_ancestor` prompt + add a `research_lifecycle` prompt; (d) error semantics: `invalidParams` → -32602, shared `{"error":"profile_not_found"}` body for profile-keyed reads; (e) delete stale "no per-gate breakdown" claims.
- **MC2 — profile truth** : `profileDetail` gains middle/nick/married/mothers-maiden names, death_location, death_year_latest, decoded attributes, external_identifiers, name_forms; relationships block gains direction + inline marriage_date on spouse rows; `relationshipsForProfile` gains marriage range/qualifier; `allProfiles` splits first/last name + adds death_location.
- **MC3 — evidence truth**: `user_status` + `gates_json` on get_scored_records and the evidence resource; citation_json/evidence_quality/fact_confidence/added_at on confirmed_facts; evidence_convergence read; `not_duplicate_of` (dismissed_duplicates) on get_profile; **E4 edge provenance** on the relationships resource (relationship-keyed field_sources incl. existence rows); life_events gain details_json + sources_json + sensitive flag.
- **MC4 — audit findings**: app-side migration `v55_audit_findings` (snapshot persisted after each in-app audit pass: rule_id, profile_id, severity, message, computed_at) + MCP read (`ancestor://audit_findings` / per-profile filter) that reports `computed_at` so staleness is honest.
- **MC5 — tree-wide + lifecycle conveniences**: tree-wide `pending_facts` + `disputes?status=open` resources; `recent?since=` change feed (union of timestamped rows); previously-dropped timestamps mapped (leads created_at, negative searched_at + v42 result_kind/hit_count); lead ranking join generalised from get_fs_hints to all sources + leads limits/status filter on get_profile (default `new,investigating`); `get_project_info`; workbench reads (notes incl. FTS search, open questions); name_equivalences resource; `get_wikitree_contributions`; `get_run_status` no-arg latest mode + queued>15s "open the app" hint + app-running caveat on kick_off_research; find_path hops carry name/gender/birth-year + disconnected-vs-hop-limit reason; treeGaps gains last_research_at; list resources drop `.prettyPrinted`.
- **MC6 — reader profile**: `ANCESTOR_MCP_PROFILE=reader` filters toolsList/callTool to reads + triggers (kick_off_research, get_run_status/result, FS reads + request_fs_hints); default `dev` unchanged. App CLAUDE.md tool list refreshed.

## Invariants

Reader profile ships NOTHING that writes tree data (no submit_*, approve/promote/dismiss, delete_project, flag_audit_override, add_workbench_note); triggers write staging rows only. Sensitive life events carry their flag; attachments file contents never serve. Payload changes are additive for dev callers except the get_profile leads default (deliberate: the unbounded firehose was the defect).
